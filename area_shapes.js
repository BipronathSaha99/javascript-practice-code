// Area of Shapes 

// Area of a Triangle
/*
//input
let base, height, area;
base = parseFloat(prompt("Enter the Base:"));
height =parseFloat(prompt("Enter the Height: "));

//calculation
area = (base*height)/2;

//output
console.log(`Area of a Triangle is = ${area} sq.unit`);
*/
// Area of Parallelogram
/*
//input
let base, height, area;
base = parseFloat(prompt("Enter the Base:"));
height =parseFloat(prompt("Enter the Height: "));

//calculation
area = (base*height);

//output
console.log(`Area of a Parallelogram is = ${area} sq.unit`);
*/

// Area of Circle

//input
let radius, area;
const pi = 3.14159;

radius = parseFloat(prompt("Enter the radius: "));

//calculation
area = pi*(radius**2);

//output
console.log(`Area of a Circle is = ${area} sq.unit`);
